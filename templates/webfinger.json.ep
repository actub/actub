{
    "subject": "acct:<%= $id %>@<%= $domain %>",
    "aliases": [
        "<%= $host %>/<%= $id %>"
    ],
    "links": [
        {
            "rel": "http://webfinger.net/rel/profile-page",
            "type": "text/html",
            "href": "<%= $host %>/<%= $id %>"
        },
        {
            "rel": "self",
            "type": "application/activity+json",
            "href": "<%= $host %>/<%= $id %>"
        }
    ]
}
